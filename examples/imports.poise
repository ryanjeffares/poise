import std::int as int;
import lib::lib as lib;

func main() {
    println(int::sqrt(81));
    println(int::abs(-2));
    println(int::pow(2, 8));

    lib::say_hello();
    println(typeof(lib::say_hello));
    println(typeof(int::sqrt) == typeof(lib::say_hello));
}