func main() {
    var a: Int = 2;
    var b: String = "Hello";
    final c: Bool = true;

    assert(a == 2);
    assert(b == "Hello");
    assert(c);
    assert(b * a == "HelloHello");
    assert((b * a) or c);
}
